import React, {useEffect} from 'react';
import {AppRoutes} from './app/routes';

function App() {
  useEffect(() => {
  }, []);

  return (
    <div className="App" >
        <AppRoutes/>
    </div>
  );
}

export default App;